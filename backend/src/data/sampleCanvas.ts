/**
 * 新用户示例画布数据
 * 由产品负责人手动制作后导出，粘贴到此处
 *
 * 使用方式：在 StoragePage 的 dev 工具栏点击"导出画布"，
 * 将得到的 JSON 中 canvasItems 和 canvasEdges 的值分别填入下方
 */

// TODO: 将导出的 canvasItems 数据粘贴到这里
export const sampleCanvasItems: Record<string, any> = {
  'canvas-root': {
    id: 'canvas-root',
    type: 'container',
    name: '工作流画布',
    parentId: '',
    position: { x: 0, y: 0 },
    size: { width: 3200, height: 1800 },
    collapsed: false,
    childrenIds: [],
    color: 'rgba(139, 92, 246, 0.15)'
  }
}

// TODO: 将导出的 canvasEdges 数据粘贴到这里
export const sampleCanvasEdges: Record<string, any> = {}
