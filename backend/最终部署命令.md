# 最终部署命令（桌面密钥文件）

> 服务器IP：**47.93.218.80**
> 密钥文件位置：**/Users/star/Desktop/工作流网站**（或 .pem）
>
> 直接复制粘贴即可！

---

## 完整部署流程

### 步骤 1：进入项目目录

```bash
cd /Users/star/Desktop/开发文件夹/workflow-platform/backend
```

---

### 步骤 2：设置密钥权限

如果文件名有 .pem：
```bash
chmod 400 /Users/star/Desktop/工作流网站.pem
```

如果没有 .pem：
```bash
chmod 400 /Users/star/Desktop/工作流网站
```

---

### 步骤 3：上传脚本

如果文件名有 .pem：
```bash
scp -i /Users/star/Desktop/工作流网站.pem aliyun-setup.sh root@47.93.218.80:/root/
```

如果没有 .pem：
```bash
scp -i /Users/star/Desktop/工作流网站 aliyun-setup.sh root@47.93.218.80:/root/
```

**成功显示：**
```
aliyun-setup.sh                100%   5KB   1.2MB/s   00:00
```

---

### 步骤 4：登录服务器

如果文件名有 .pem：
```bash
ssh -i /Users/star/Desktop/工作流网站.pem root@47.93.218.80
```

如果没有 .pem：
```bash
ssh -i /Users/star/Desktop/工作流网站 root@47.93.218.80
```

**成功登录显示：**
```
[root@iZ... ~]#
```

---

### 步骤 5：运行部署脚本（在服务器上）

```bash
cd /root
```

```bash
chmod +x aliyun-setup.sh
```

```bash
./aliyun-setup.sh
```

**等待 2-5 分钟，看到 "🎉 部署完成！"**

---

### 步骤 6：退出服务器

```bash
exit
```

---

### 步骤 7：初始化数据库（回到本地）

```bash
cd /Users/star/Desktop/开发文件夹/workflow-platform/backend
```

```bash
npm run db:generate
```

```bash
npm run db:push
```

---

### 步骤 8：启动测试

```bash
npm run dev
```

**在新终端测试：**
```bash
curl http://localhost:3000/health
```

**成功显示：**
```json
{"status":"healthy"}
```

---

## 快捷登录命令（后续使用）

登录服务器查看状态：
```bash
ssh -i /Users/star/Desktop/工作流网站.pem root@47.93.218.80
```

查看数据库状态：
```bash
cd /root/workflow-db
docker-compose ps
```

查看日志：
```bash
cd /root/workflow-db
docker-compose logs -f
```

---

## 可能的错误和解决方案

### 错误1: "Permission denied (publickey)"
**解决：**
```bash
chmod 400 /Users/star/Desktop/工作流网站.pem
```

### 错误2: "No such file or directory"
**原因：** 文件名不对（有没有 .pem）
**解决：** 执行下面命令查看真实文件名
```bash
ls -la /Users/star/Desktop/工作流网站*
```

### 错误3: "Host key verification failed"
**解决：** 输入 yes 后回车

---

## ✅ 检查清单

- [ ] 进入项目目录
- [ ] 设置密钥权限 (chmod 400)
- [ ] 上传脚本成功
- [ ] 登录服务器成功
- [ ] 运行脚本看到 "🎉 部署完成！"
- [ ] 退出服务器
- [ ] npm run db:generate 成功
- [ ] npm run db:push 成功
- [ ] npm run dev 启动
- [ ] curl /health 返回 healthy
